{{- if .Values.backend.enabled }}

apiVersion: v1
kind: Service
metadata:
  name: space.{{ .Release.Name }}-backend
spec:
  type: {{ .Values.backend.service.type }}
  selector:
    app: space.{{ .Release.Name }}-backend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http-server
      nodePort: {{ .Values.backend.service.nodePort }}
{{- end }}